# Local Development - uses Vite proxy (leave empty or use relative path)
# The proxy in vite.config.js handles /api requests to localhost:8000

# For production, set this to your Sevalla backend URL:
# VITE_API_URL=https://your-backend.sevalla.app/api



