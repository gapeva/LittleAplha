# ===========================================
# Little Alpha - Production Environment Config
# ===========================================

# Database Configuration (PostgreSQL for Production)
# Sevalla will provide this DATABASE_URL automatically when you add a PostgreSQL database
DATABASE_URL=postgresql://user:password@hostname:5432/little_alpha

# Security
# IMPORTANT: Generate a secure key with: openssl rand -hex 32
SECRET_KEY=change_this_to_a_secure_random_string_in_production
ACCESS_TOKEN_EXPIRE_MINUTES=43200
ALGORITHM=HS256

# CORS Configuration
# Add your Sevalla frontend URL here (no spaces after commas)
ALLOWED_ORIGINS=https://your-app.sevalla.app,https://littlealpha.com

# Port (Sevalla uses PORT env variable)
PORT=8000
