# Database Configuration
# Sevalla/Render/Heroku will provide this URL automatically
DATABASE_URL=postgresql://user:password@hostname:5432/little_alpha

# Security
# Run `openssl rand -hex 32` to generate
SECRET_KEY=change_this_to_a_secure_random_string_in_production
ACCESS_TOKEN_EXPIRE_MINUTES=43200
ALGORITHM=HS256

# CORS Configuration
# No spaces after commas. Add your Vercel URL here.
ALLOWED_ORIGINS=https://little-alpha.vercel.app,http://localhost:5173
